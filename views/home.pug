extends layouts/main
include mixins/videoBlock

block content 
    .videos
        each videoItem in videosFiles 
            +videoBlock({
                title: videoItem.title,
                views: videoItem.views,
                videoFile: videoItem.videofile
            })
